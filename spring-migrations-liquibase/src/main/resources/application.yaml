spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/liquibase
    username: lazarev
    password: root
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none

  liquibase:
    enabled: true